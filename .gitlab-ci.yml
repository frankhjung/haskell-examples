---

stages:
  - build

build:
  stage: build
  except:
    - tags
  image:
    name: frankhjung/haskell:9.4.7
  script:
    - make setup check build exec
  artifacts:
    paths:
      - dist-newstyle/
  cache:
    key: ${CI_COMMIT_REF_NAME}
    paths:
      - dist-newstyle/
